# Starting level: basics known, query keys and caching are gaps

User has used `useQuery`/`useMutation` in real code before, but disclosed being shaky on query key design, caching/staleness internals, and advanced patterns (invalidation strategy, infinite queries, optimistic updates). Matters for future sessions: skip re-teaching hook basics from zero; slow down and add extra practice specifically around query keys (post #8) and caching internals (post #19) when we reach them.
