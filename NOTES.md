# Notes

- User's own words: "純粹興趣 / 打基礎" (pure interest, building foundation) — no work deadline, no interview pressure. Pace lessons for depth, not speed.
- Starting level: "用過但不熟" (used it, not fluent) — knows basic `useQuery`/`useMutation`, but query key design, caching internals, and advanced patterns are the known weak spots. Don't re-explain "what is useQuery" from zero; do slow down on query keys/caching/staleness.
- User communicates in Traditional Chinese (Taiwan). Write lessons and conversation in Traditional Chinese; code/API names stay in English as-is.
- Following the TkDodo series in published order (see RESOURCES.md) — each lesson maps to one post, roughly.
